[package]
name = "chime"
version = "0.1.0"
edition = "2021"

[dependencies]
flux-proc-macro = { path = "flux-proc-macro" }
glam = { version = "0.24.2", optional = true }
bevy = { version = "0.12.1", optional = true }

[features]
glam = ["dep:glam"]
bevy = ["dep:bevy"]
